public class BulbNode { // BulbNode.java
	 
	protected Bulb data; // Protected instance variable for data
	protected BulbNode next; // Protected instance variable for next

	public BulbNode(Bulb b) { // One argument constructor
		this.data = b;
	}
}
